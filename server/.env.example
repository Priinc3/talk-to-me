# Vertex AI backend config — copy to .env and fill in
# The service account JSON lives at vertex-ai-key.json (gitignored) OR set VERTEX_SA_JSON
GCP_PROJECT_ID=project-271c2912-087b-4e51-aa3
GCP_LOCATION=us-central1
PORT=3000

# Optional overrides
# VERTEX_SA_JSON={"type":"service_account",...}
# CONFIG_FILE=config.json
